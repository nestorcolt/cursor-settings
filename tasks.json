// tasks.json
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "close-terminal",
            "command": "workbench.action.terminal.kill",
            "type": "process"
        }
    ]
}